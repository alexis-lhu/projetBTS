<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Categorie{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/swiper.min.css') }}">
    <style>
        {# Votre style personnalisé ici #}

        body {
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #fff;
            text-align: center;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
        }

        .logo {
            align-self: flex-start;
        }

        .logo img {
            width: 125px;
        }

        .buttons {
            display: center;
            justify-content: center;
            flex-grow: 1;
        }

        .buttons a {
            color: #000;
            text-decoration: none;
            margin: 5px 35px;
            font-family: 'Jomhuria', sans-serif;
            font-weight: bold;
            font-size: 35px;
        }

        .icons {
            display: flex;
            justify-content: flex-end;
        }

        .icons img {
            width: 40px;
            margin: 0 10px;
            cursor: pointer;
        }

        .header-bar {
            height: 5px;
            background-color: #ccc;
            width: 100%;
            position: absolute;
            bottom: 0;
            left: 0;
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
            z-index: 1;
            right: 0;
            text-align: left;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .icons-small img {
            width: 40px;
            margin: 0 5px;
            cursor: pointer;
        }

        .dropdown-content a {
            display: block;
            padding: 10px;
            color: #000;
            text-decoration: none;
            font-family: 'Jomhuria', sans-serif;
            font-weight: bold;
        }

        /* Ajoutez d'autres styles spécifiques à la page "Categorie" si nécessaire */
    </style>
</head>
<body>
    <header>
        <div class="logo">  
            <img src="{{ asset('images/Logo2.png') }}" alt="Logo">
        </div>
        <div class="buttons">
            <a href="{{ path('home') }}">ACCUEIL</a>
            <a href="{{ path('categorie', {'type': 'homme'}) }}">HOMME</a>
            <a href="{{ path('categorie', {'type': 'femme'}) }}">FEMME</a>
            <a href="{{ path('categorie', {'type': 'enfant'}) }}">ENFANT</a>
            <a href="{{ path('categorie', {'type': 'accessoire'}) }}">ACCESSOIREeeeeeeeeeee</a>
        </div>

        <div class="icons">
               <a href="{{ path('categorie', {'type': 'panier'}) }}">
        <img src="{{ asset('images/Panier.png') }}" alt="Panier">
    </a>
            <div class="dropdown">
                <div class="icons-small">
                    <img src="{{ asset('images/Compte.png') }}" alt="Compte" style="width: 30px;">
                </div>
                <div class="dropdown-content">
                    <a href="#">Se connecter</a>
                    <a href="#">S'enregistrer</a>
                </div>
            </div>
        </div>
        <div class="header-bar"></div>
    </header>

    {# Ajoutez ici le contenu spécifique à la page "Categorie" #}

    <script src="{{ asset('js/swiper.min.js') }}"></script>
    <script src="{{ asset('js/SwipeScript.js') }}"></script>
</body>
</html>
